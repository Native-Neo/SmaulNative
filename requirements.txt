torch>=2.0
ninja>=1.11
safetensors>=0.4
huggingface_hub>=0.24
pyarrow>=14.0
fastapi>=0.115
pydantic>=2.0
uvicorn>=0.34

# Optional features (guarded imports with helpful errors when missing):
# gguf  -- convert_linear_to_gguf.py GGUF export only (`pip install gguf`)
